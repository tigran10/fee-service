version: '2'

services:
  fee-service:
    image: acuma/fee-service:latest
    restart: always
    ports:
      - "4026:8080"
    logging:
      driver: json-file
      options:
        max-size: "100m"
        max-file: "2"
